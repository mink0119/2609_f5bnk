# Gateway API TCPRoute kind 미지원. BNK native: TCP + L4Route backendRefs.weight
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: tcp-gw
  namespace: web
spec:
  gatewayClassName: f5-bnk-gateway-class

  addresses:
    - type: IPAddress
      value: 40.30.20.20

  listeners:
    - name: bnk-listener
      protocol: TCP
      port: 80

      allowedRoutes:
        namespaces:
          from: All
        kinds:
          - kind: L4Route
            group: gateway.k8s.f5net.com

---
apiVersion: gateway.k8s.f5net.com/v1
kind: L4Route
metadata:
  name: tcp-weight-route
  namespace: web
spec:
  protocol: TCP
  parentRefs:
    - name: tcp-gw
      sectionName: bnk-listener

  rules:
    - backendRefs:
        - name: coffee-pool
          kind: Pool
          group: k8s.f5net.com
          weight: 70
        - name: tea-pool
          kind: Pool
          group: k8s.f5net.com
          weight: 30

---
apiVersion: k8s.f5net.com/v1
kind: Pool
metadata:
  name: coffee-pool
  namespace: web
spec:
  members:
    - address: "30.0.0.10"
      port: 80

---
apiVersion: k8s.f5net.com/v1
kind: Pool
metadata:
  name: tea-pool
  namespace: web
spec:
  members:
    - address: "30.0.0.11"
      port: 80
